import 'package:flutter/material.dart';

class NewsYao extends StatelessWidget {
  const NewsYao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
