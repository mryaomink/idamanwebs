import 'package:flutter/material.dart';

class AppsYao extends StatelessWidget {
  const AppsYao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
